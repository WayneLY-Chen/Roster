# 名單匠 Roster

蒐集**公開**的台灣 B2B 廠商聯絡資料，清理、驗證、建檔、匯出，
並可透過你自己的 Gmail 寄送開發信。

Python 3.12+ ｜ 桌面應用程式（PySide6）｜ 本機 SQLite ｜ Windows / macOS / Linux ｜ 也可用命令列操作

最新版本 **v1.27.2** — 這一版改了什麼見 [`CHANGELOG.md`](CHANGELOG.md)

> **使用前請先閱讀本文件最後的[免責聲明](#免責聲明)。**
> 本工具僅供蒐集網站公開顯示的商業聯絡資訊，使用者須自行確認每一個爬取目標的合法性並自負全部責任。

---

## 安裝

需要 **Python 3.12 或更新版本**（[官方下載](https://www.python.org/downloads/)。
Windows 安裝時記得勾選 *Add python.exe to PATH*）。

打開你的作業系統對應的資料夾，**雙擊「安裝」**，剩下的它會自己做完：

| 你的系統 | 打開這個資料夾 | 雙擊 |
|---|---|---|
| Windows | `Windows/` | `安裝.bat` |
| macOS | `macOS/` | `安裝.command` |
| Linux | `Linux/` | `./install.sh` |

安裝程式會建立虛擬環境、下載套件（約 150 MB，需要幾分鐘），
macOS 還會順便產生 `Roster.app`。可以重複執行，已經裝好的部分會直接跳過。

### 日常啟動

| | Windows | macOS | Linux |
|---|---|---|---|
| 開啟程式 | `Windows/啟動.bat` | `macOS/啟動.command` 或 `Roster.app` | `Linux/start.sh` |
| 命令列 | `Windows/命令列.bat` | `macOS/命令列.command` | `Linux/console.sh` |
| 更新到新版 | `Windows/更新.bat` | `macOS/更新.command` | `Linux/update.sh` |

### 更新到新版本

**不用重新 clone 一次**，雙擊「更新」就好。它會抓最新版、補裝新增的套件，
最後告訴你從哪一版更新到哪一版。

你的東西一個都不會動——資料庫、附件、匯出的檔案、備份、信件範本、
`user_settings.yaml` 全都不在 git 的管轄範圍內。`config.yaml` 有進 git，
所以你改過的話會先把你的修改收起來、更新完再放回去。資料庫要加的新欄位，
程式下次啟動時自己會補上，不用另外做什麼。

程式**自己也會提醒你**：開啟時（一天最多一次）會看一下 GitHub 上有沒有新版，
有的話跳一個視窗問你要不要更新，按「更新」就地更新完，重開程式即可。沒有新版
時完全不出聲。不想要的話把 `config.yaml` 的 `app.check_for_updates` 設成
`false`，「更新資訊」頁還是有一顆手動的「檢查更新」。

> **手上這一版還沒有「更新」這個檔案？** 那是舊版才會這樣。開一次
> 「命令列」執行 `git pull`，更新完之後它就在那裡了，以後都用雙擊的。

當初是下載 ZIP 而不是用 git clone 的話，資料夾裡沒有 `.git`，更新檔會直接
說清楚，並列出哪些檔案是你的、要先搬出來再換。改用 git 下載一次就不會再有
這個問題（也順便省掉下面那個 macOS 的隔離標記問題）。

#### macOS 出現「Apple 無法驗證是否為惡意軟體」

macOS 會替**所有**從瀏覽器下載的檔案貼上隔離標記，沒有付費的 Apple 開發者
簽章就一律顯示這個訊息。macOS 15 (Sequoia) 起，以前那個「按右鍵 →『打開』」
的做法已經被 Apple 拿掉，右鍵沒有用。

**一行指令解決**（終端機）：

```bash
xattr -dr com.apple.quarantine ~/Desktop/Roster
```

路徑換成你資料夾實際的位置——打完 `com.apple.quarantine` 留一個空白，
再把資料夾拖進終端機視窗，路徑會自動填好。之後雙擊就正常了。

**或者一開始就用 git 下載**，clone 下來的檔案不算「從網路下載」，不會被貼
標記，下載完直接雙擊就能用：

```bash
git clone https://github.com/WayneLY-Chen/Roster.git
```

安裝程式跑完會自動解除整個資料夾的隔離標記，所以只要能讓「安裝.command」
執行一次（上面任一種方法），之後的啟動都不會再被擋。
`macOS/開不起來 請先看這個.txt` 裡有同樣的說明。

> `Roster.app` 是 `macOS/建立App.command` 產生的，只是一層薄殼——執行時仍然
> 使用專案資料夾裡的 `.venv`，所以改了程式碼不用重新產生，代價是它不能單獨
> 搬到別台電腦。要那樣的話得用 PyInstaller 打包（設定檔是
> `packaging/roster.spec`）。

### 內建瀏覽器

有一大半的名錄是「網頁先送空殼，再用程式把資料填上去」，直接讀原始碼一個字
都看不到。那種網站要用內建瀏覽器才抓得到，**安裝程式會自動下載**（約 120 MB）。

那一步失敗不影響其他功能，之後想補裝的話，開啟命令列執行：

```bash
python -m playwright install chromium
```

程式會自己判斷哪些網站需要它，不必手動切換。

### 搬動資料夾

啟動腳本都以自己所在的位置推算路徑，整個資料夾搬到哪裡、改成什麼名字都能用。

> 如果搬動之後 `pip`、`pytest` 這些指令失效，那是 Python 虛擬環境本身
> 不可搬移的已知行為。改用 `python -m pip`、`python -m pytest` 即可，
> 或重新執行一次安裝檔。

---

## 五分鐘上手

### 先確認整條流程正常（不用連網）

1. 依上方步驟建好環境，啟動程式
2. 到「**匯入**」頁 → 按「**下載範例檔**」→ 存成 Excel
3. 打開那個檔案，第二個分頁有逐欄說明。照著填幾家公司，存檔
4. 回到「**匯入**」頁 → 「選擇檔案…」選它 → 確認欄位對應 → 按「**匯入**」
5. 到「**公司**」頁看資料，再到「**匯出**」頁匯出來對一次

這一輪完全不會連到任何網站，用來確認安裝沒問題。

### 開始爬第一個名錄

1. 到「**爬取**」頁 → 按「**＋ 自訂網址…**」
2. 貼上你有權限爬取的名錄網址 → 按「**分析網頁**」
3. 程式會告訴你抓到幾筆、有哪些欄位、這個名錄總共幾頁
4. 在「預覽抓到的資料」確認內容 → 填來源名稱 → 「**儲存並立即爬取**」

程式**不預先附任何爬取來源**——要爬哪些網站是你自己的決定，
而且你必須先確認那個網站允許被爬取（見文末的[免責聲明](#免責聲明)）。

---

## 每一頁在做什麼

| 頁面 | 用途 |
|---|---|
| **AI 助手** | 接一個語言模型進來。打關鍵字找值得抓的網站、貼網址讓它讀那一頁把公司抽出來（每個值都會回頭對回原文，對不到就丟掉）、用中文問你自己的名單（唯讀，答案一定附上用了哪些條件）。也可以接外部工具（MCP）讓它做這支程式自己不做的事。抓之前、用工具之前，一定會先讓你確認一次 |
| **儀表板** | 總覽：公司總數、信箱數、本週新增、疑似重複、業務階段分佈、最近爬取紀錄 |
| **公司** | 主要工作區。搜尋、篩選（含依收集日期）、新增、編輯、刪除、找出重複並合併 |
| **聯絡人** | 跨公司搜尋所有具名聯絡人 |
| **爬取** | 執行爬取、選擇要收集哪些欄位、貼網址建立自訂來源、補抓信箱、補公司登記資料、分批補齊公司資料、重新驗證 |
| **匯入** | 從 CSV / Excel / JSON 匯入既有名單。別人給的名冊不必先整理——上面壓著大標題、名冊在第二張工作表、欄名叫「工廠名稱」都讀得進來。可在匯入後自動補齊，附範例檔下載 |
| **匯出** | 匯出 Excel / CSV / JSON，可挑欄位、下篩選條件 |
| **郵件** | 編輯信件樣板、加附件、產生收件名單、預覽、寄送。還可以讀一次自己的信箱：把退信找出來（寄不到的標起來就不會再寄）、把回信的公司推到「已聯絡」、把說「不要再寄」的標成請勿聯絡 |
| **日誌** | 依分類查看執行紀錄（爬取／資料庫／匯出／介面／錯誤） |
| **設定** | Gmail 帳號、AI 模型、自動排程、外觀、設定總覽、備份與還原 |
| **更新資訊** | 這一版改了什麼（內容就是 [`CHANGELOG.md`](CHANGELOG.md)），以及一顆「檢查更新」 |
| **反饋** | 直接在程式裡把問題回報給作者，可附截圖 |

---

## 詳細說明

上面那張表是「哪一頁做什麼」。真的要動手時，各自的說明在這裡：

| 文件 | 內容 |
|---|---|
| [爬取一個網站](docs/爬取.md) | 讓程式去找名錄、用精靈建立來源、PDF 名冊、要按按鈕才出現的資料、只有查詢框的名錄、抓到的東西不對怎麼修、補抓信箱、補公司登記資料、只有公司名稱時怎麼補齊、分批跑與自動接續 |
| [整理與匯出名單](docs/名單.md) | 會存哪些欄位、名單品質分數、資料清理、去重、依日期整批刪除、匯入（別人給的檔案不用先整理）與匯出 |
| [寄送開發信](docs/寄信.md) | Gmail 應用程式密碼、信件樣板、附件庫、產生名單與寄送、內建的保護機制 |
| [自動排程](docs/排程.md) | 定時爬取、定時寄信 |
| [AI 助手](docs/AI.md) | 接一個語言模型進來：本機的 Ollama 或線上的 OpenRouter／Anthropic、自己寫指示、接外部工具（MCP）、隱私與那條「不准違規爬網站」為什麼不是靠指示守的 |

---

## 命令列

開啟命令列（`Windows/命令列.bat`、`macOS/命令列.command`、`Linux/console.sh`）之後：

```bash
python main.py --help                  # 列出所有指令
python main.py gui                     # 開啟視窗介面
python main.py crawl --list            # 列出已設定的來源
python main.py crawl -s 來源名稱        # 爬取指定來源
python main.py crawl --url URL         # 直接爬一個網址
python main.py enrich                  # 補抓信箱（要先有網址）
python main.py registry                # 用統一編號補公司登記資料
python main.py complete                # 只有公司名稱也能補：統編、官網、信箱、電話、聯絡人
python main.py verify                  # 重新驗證所有信箱
python main.py export -f excel         # 匯出
python main.py stats                   # 資料庫摘要
python main.py schedule                # 前景執行排程，Ctrl+C 停止
python main.py security                # 檢查資料安全設定
```

---

## 設定檔

| 檔案 | 內容 |
|---|---|
| `config.yaml` | 主設定，寫滿註解。爬取行為、驗證、匯出、寄信、排程 |
| `user_settings.yaml` | 在介面上改的設定存這裡，不會動到 `config.yaml` 的註解 |
| `custom_sources.yaml` | 用精靈建立的爬取來源 |
| `.env` | 選用。憑證優先存進系統的憑證保管庫，這裡只是後備 |

| 資料夾 | 內容 |
|---|---|
| `data/` | SQLite 資料庫 |
| `output/` | 匯出的檔案 |
| `attachments/` | 信件附件 |
| `templates/mail/` | 信件樣板與內嵌圖片 |
| `backups/` | 自動與手動備份 |
| `logs/` | 執行紀錄 |

**備份**：「設定」頁可以隨時建立備份、還原、或刪除不要的備份。每日與每週備份
會依保留數量自動清理，手動備份則會一直留著。

**資料庫加密**：`config.yaml` 的 `database.encrypt` 開啟時，信箱、電話、地址、
聯絡人姓名等個資欄位會以 AES-GCM 加密存放，金鑰放在系統的憑證保管庫。
換電腦前記得到「設定」頁「**備份金鑰**」，否則資料無法解密。

---

## 回報問題

側邊欄最下面的「**反饋**」頁：填上你的信箱（選填）、寫下遇到的問題、
可以附截圖，按送出。版本與作業系統資訊會自動附上，**不含你的資料夾路徑**。

已經設定好 Gmail 帳號的話會直接寄出；還沒設定的話會開啟你自己的郵件軟體
並把內容填好——那種情況下截圖要自己拖進去。

也可以直接到 [GitHub Issues](https://github.com/WayneLY-Chen/Roster/issues) 開一則。

---

## 授權

本專案以 **MIT License** 發佈，可自由使用、修改、重新發佈，
但須保留原著作權聲明。完整條文見 [`LICENSE`](LICENSE)。

授權涵蓋的是**這個工具本身**，不包含你用它蒐集到的任何資料。
那些資料仍受《個人資料保護法》與目標網站使用條款規範。

「補公司登記資料」用到的資料另外適用**政府資料開放授權條款－第 1 版**：

> 公司登記資料來源：經濟部商業司 商工登記公示資料（政府資料開放授權條款－第 1 版）

該條款允許不限目的（含商業）、免授權金的利用，但顯名標示是強制義務——
「未盡顯名標示義務者，視為自始未取得開放資料之授權」。所以這段標示會固定
出現在設定頁、公司詳細資料，以及每一份含有登記資料的匯出檔，請不要移除。

---

## 免責聲明

**本工具僅供蒐集網站「公開顯示」的商業聯絡資訊。使用者須自行確認每一個爬取
目標的合法性，並自負全部責任。**

**嚴禁**以本程式從事下列行為：

1. 爬取 `robots.txt` 明文禁止爬取之網站或路徑
2. 爬取網站使用條款或著作權聲明中禁止自動化蒐集之內容
3. 以任何方式繞過登入驗證、付費牆、CAPTCHA、速率限制或其他技術保護措施
4. 解析經 JavaScript、Cloudflare 或其他機制**刻意混淆**之聯絡資訊——
   混淆本身即代表對方明示不希望被自動蒐集
5. 蒐集自然人之個人資料，或蒐集與商業往來無關之資訊
6. 以過高頻率請求而影響目標網站正常運作
7. 將蒐集所得資料轉售、公開散布，或用於騷擾、詐騙等不法用途

本程式預設遵守 `robots.txt`、插入請求延遲、並誠實表明 User-Agent。這些設定
可以調整，但**調整設定之行為及其後果，由使用者自行承擔**。

寄送開發信另受《個人資料保護法》規範：只能寄給你有正當來源取得的商業聯絡
資訊且用途須與業務相關，必須提供可執行的退訂方式（程式已強制附加），
收到退訂要求後應立即停止聯絡。

本程式係「依現況」提供，不附任何明示或默示之擔保。作者／提供者對於使用者
如何使用本程式**不具任何控制能力，亦不負任何監督義務**。使用者若以本程式
爬取不應爬取之網站、違反目標網站使用條款、違反《個人資料保護法》或其他法令，
其一切法律責任與後果（包含民事賠償、行政罰鍰及刑事責任）均由**使用者本人**
自行承擔，**作者／提供者一概不負責任**。

若不同意上述任何內容，請立即停止使用本程式並刪除之。

完整條文可在程式的「設定」頁查看，或見 [`core/legal.py`](core/legal.py)。
