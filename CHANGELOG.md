# 更新紀錄

## 1.6.2 — 2026-08-05

### macOS 的下拉選單文字被切掉

「（全部啟用）」顯示成「（全部啟用」。原生樣式的下拉箭頭區比 Qt 的預估值寬，
全形括號又比字寬估計值再寬一些，加起來差的那幾個 px 就吃掉最後一個字。

下拉選單的寬度上限原本是寫死的 240px。Windows 上綽綽有餘，macOS 的系統字大
一號就不夠了。改成跟著字寬算，並且在版面把它壓窄時也不讓文字被截斷。

### 找得出 macOS 裝不起來的原因

`Roster.app` 建立失敗時，安裝程式原本把錯誤訊息丟掉，畫面只寫「建立失敗」
——連我們自己都看不到為什麼。現在會把完整錯誤印出來。

- `macOS/` 新增「**診斷.command**」：把系統版本、Python、虛擬環境、檔案權限、
  隔離標記、以及重新建立 `Roster.app` 的完整輸出一次列出來。出問題時雙擊它，
  把輸出貼出來就能定位。
- `建立App.command` 產生完會自己驗證一次，不再有「畫面寫已建立、實際是空殼」
  的情況。
- 修掉一個會讓 `建立App.command` 靜悄悄中止的寫法（`sed | head` 在
  `set -o pipefail` 之下可能因為 SIGPIPE 讓整條管線算成失敗）。
- 「開不起來 請先看這個.txt」補上終端機視窗不自動關閉的設定位置。

---

## 1.6.1 — 2026-08-05

### macOS 裝不起來

雙擊「安裝.command」會出現「Apple 無法驗證是否為惡意軟體」，而且**按右鍵
「打開」也沒有用**——macOS 15 (Sequoia) 把那個繞法拿掉了，剩下的官方途徑是
到系統設定裡對每一個檔案分別按一次「仍要打開」。

- 安裝程式執行完會自動解除整個資料夾的「下載自網路」隔離標記，之後啟動
  就不會再被擋。只動這個資料夾，不改任何系統設定。
- `macOS/` 裡多了「**開不起來 請先看這個.txt**」，寫著怎麼讓安裝程式跑起來
  的第一次（一行終端機指令，或改用 `git clone` 從一開始就避開）。
- README 原本寫「按右鍵 →『打開』」，那在新版 macOS 上是錯的，已改掉。

### macOS 安裝程式修正

上一版把啟動檔分到各自的作業系統資料夾時，`安裝.command` 裡的檔名沒有跟著
改，指到的還是搬動前的舊位置：

- **`Roster.app` 從來沒有被建立過** — 它去執行一個已經不存在的檔案，而失敗
  是被吃掉的，畫面只會顯示「建立失敗，用 start.command 也可以」，
  而 `start.command` 也已經不叫這個名字了。
- 安裝完的說明寫著錯誤的檔名。
- `Roster.app` 的版本號寫死在腳本裡（停在 1.3.0），改成從程式本身讀。

---

## 1.6.0 — 2026-08-05

這一版是為了一類爬不動的網站：2000 年代的公會名錄。它們整頁只有表格排版、
沒有一個 CSS class、內容是 Big5，欄位以「負責人︰王大明」這種文字形式列出。
以選擇器為主的分析對它們幾乎完全無效，程式只會回報「找不到公司名稱」。

### 其他欄位

**每個名錄有什麼欄位是那個公會決定的，不是程式能事先列完的。** 旅行公會列
「會員代表」「入會年月日」「註冊編號」，化工公會列「代理廠商及代銷產品」。
以前這些會被丟掉——使用者在網頁上看得到，在程式裡卻找不到。

現在抓到什麼就留什麼，照名錄上原本的名稱存起來：

- 公司「詳細資料」裡多了「**其他欄位**」一區，看得到、能改，也能自己新增
- 匯出時每個自由欄位各佔一欄
- 匯入時對應不到固定欄位的欄也收得回來，不再直接丟掉

### 讀得懂「標籤︰值」的排版

`負責人︰`、`Tel︰`、`統一編號︰`、`營業範圍︰` 這些寫在文字裡的欄位現在讀得
出來了。認得出的標籤自動填進對應欄位（負責人→聯絡人、FAX→傳真），認不出的
就成為上面說的自由欄位。

這件事沒有辦法用 CSS 選擇器做——那種頁面裡「負責人」跟它的值只是同一格裡的
兩段文字，沒有任何標記分得開它們。

### 修正

- **Big5 網站整頁變亂碼** — 這些站的 HTTP 標頭常常不附編碼，只寫在頁面自己
  的 `<meta>` 裡。少了它，中文會被當成 UTF-8 解讀而變成亂碼——而亂碼的公司
  名稱看起來仍然「有值」，不會報錯，只會安靜地存進一堆看不懂的字。現在改讀
  頁面自己的宣告。
- **一筆資料被拆成好幾筆** — 判斷「要不要往結構更裡面找」的條件太寬鬆，
  會把一張公司卡片拆成它裡面的每一個連結、或把一筆紀錄拆成它的每一列。
  現在會先確認拆出來的東西本身仍然像一家公司。
- **沒有標題與 class 的頁面找不到公司名稱** — 舊式名錄的公司名稱就是卡片裡
  的第一段文字，而原本的判斷只看標題標籤、連結與 class 名稱。
- **清單選擇器選到排版用的表格** — `table` 這種只有標籤名的選擇器存起來之後
  會連版面表格一起選中。現在會自動加上範圍限定，並驗證加了之後選到的東西
  跟偵測時完全一致。
- **地址被當成公司名稱** — 「…中山北路二段45號」結尾的「號」也是小型商家
  名稱的常見結尾（「祥發包裝材料行」那一類），沒有分開處理的話，一個沒有
  公司名稱的頁面會拿地址頂替。
- **網址抓到別家公司的** — 有名錄在公司沒有網址時，直接把上一家的連結留在
  原地、只把顯示文字清空。照連結位址抓的話，一整串沒有網站的公司會被填上
  別人的網址，而畫面上根本看不到那個網址。現在以顯示文字為準。
- **全站共用的值不再被當成公司資料** — 信箱、電話、傳真、網址、統編這幾項
  如果每一筆抓到的都一樣，那不是這家公司的資料。
- **詳細資料視窗可以捲動** — 欄位數量現在由名錄決定，不再是固定的。

---

## 1.5.0 — 2026-08-05

### 雙擊安裝

以前第一次使用一定要開終端機打指令建虛擬環境。現在打開你的作業系統對應的
資料夾，雙擊「安裝」就好——它會檢查 Python 版本、建立虛擬環境、安裝套件，
macOS 還會順便產生 `Roster.app`。可以重複執行，已經裝好的部分會跳過。

找不到合適的 Python 時會直接告訴你去哪裡下載，而不是丟一串錯誤訊息。

### 依作業系統分資料夾

啟動檔原本全部堆在根目錄，使用者得自己分辨哪個是自己該點的。現在分成三個
資料夾，打開就知道要用哪一個：

```
Windows/   安裝.bat   啟動.bat   命令列.bat
macOS/     安裝.command   啟動.command   命令列.command   建立App.command
Linux/     install.sh   start.sh   console.sh
```

根目錄也順手整理過：打包相關的檔案移到 `packaging/`。
其餘留在根目錄的都是被慣例釘住的——`requirements.txt` 是 pip 找的位置、
`README` 與 `LICENSE` 是 GitHub 只認根目錄、`.gitignore` 是 git 只認根目錄。

---

## 1.4.0 — 2026-08-05

### 修正：每個對話框都會開兩次

點兩下開啟公司詳細資料，關掉之後它又自己開一次——每一個用雙擊開啟的視窗
都是這樣。原因是表格同時接了 `doubleClicked` 與 `activated` 兩個訊號，而
**一次雙擊會同時送出這兩個**，處理函式因此被呼叫兩次。

### macOS 可以雙擊啟動了

macOS 的 Finder 不會執行 `.sh`，它只會用文字編輯器打開——所以之前只能開
終端機打指令。現在有兩個選擇：

| 檔案 | 用途 |
|---|---|
| `start.command` | 雙擊直接啟動（會跟著開一個終端機視窗） |
| `./make_macos_app.sh` | 產生 `Roster.app`，可以放進「應用程式」或 Dock，不會開終端機 |

第一次開啟若被 Gatekeeper 擋下來，按右鍵 →「打開」即可。

### 其他

- **公司詳細資料視窗被壓扁** — 新增三個欄位之後變成 12 列，原本寫死的
  高度會把每一列的文字切掉一半。改成跟著字型度量算。
- **應用程式密碼的網址變成可點的超連結** — 要使用者自己把一長串網址抄進
  瀏覽器，是設定流程裡最容易打錯也最沒必要的一步。

---

## 1.3.0 — 2026-08-05

### 新增三個公司欄位

台灣的公協會名錄普遍會列這些，但程式原本沒有地方存：

| 欄位 | 說明 |
|---|---|
| **英文名稱** | 做外銷或對帳時要用 |
| **傳真** | 名錄上跟電話並列，很多老廠商仍在用 |
| **主要產品／代理品項** | 名錄上的「代理廠商及代銷產品」 |

三個都可以爬取、匯入、匯出、在詳細資料視窗裡編輯，也可以在信件樣板裡當
變數用（`{english_name}`、`{products}`）。

### 其他

- **公司列表加上「聯絡人」欄**。
- **空欄位不再留白** — 列表顯示「—」，詳細資料視窗顯示「（無資料）」。
  留白讓人分不出「這家沒有這項資料」與「程式沒抓到／還沒載入」。
- 詳細資料視窗重新排版，把同一類的欄位放在一起
  （公司名稱／英文名稱、電話／傳真、網址／地址）。

---

## 1.2.0 — 2026-08-05

### 新功能

- **更新資訊頁**（側邊欄）— 就是你正在看的這一頁。
- **在設定總覽直接改設定** — 點兩下或按右鍵編輯爬取引擎、爬取延遲、
  頁數上限預設值、信箱 MX 驗證、從 Gmail 收件匣抓聯絡人。
  其餘項目維持唯讀：「遵守 robots.txt」關掉會讓程式做出不該做的事，
  留在設定檔裡讓要改的人自己去看那段說明；加密切換會重寫整個資料庫。
- **單日資料匯出**（設定 → 備份）— 把某一天收集到的公司單獨存成
  Excel／CSV／JSON。這是匯出不是備份：產生的檔案可以重新匯入或交給別人，
  但沒辦法拿來還原整個資料庫（SQLite 的備份是整個檔案複製，
  「只備份某一天」還原回去會把其他日期的資料一起抹掉）。

### 變更

- **不再預先附上任何爬取來源** — 來源清單預設是空的。
  要爬哪些網站是你自己的決定，程式不預先塞範例目標進去。
  用「爬取」頁的「＋ 自訂網址…」貼上網址即可新增。
- **「每個來源頁數上限」改名為「頁數上限預設值」** — 那個值現在只是
  「來源沒指定時的預設」，原本的名稱正好是相反的意思。
- **「Gmail 讀信」改名為「從 Gmail 收件匣抓聯絡人」** — 原本的名稱看不出
  是在讀誰的信、讀來做什麼。
- **自訂網址精靈的收集設定要分析完才能動** — 順序上一定是「先分析、
  才知道有哪些欄位可以選、這個名錄有幾頁」。分析之前就讓它們可以填，
  剛好把因果顛倒過來。

### 修正

- **設定總覽仍顯示完整路徑** — 上一版只改到「資料庫」那一行，
  匯出／日誌／備份三行還是印著含使用者帳號名稱的完整路徑。
- **「刪除該日全部」按不下去** — 原本要求先在「收集日期」選一天才會啟用。
  改成永遠可以按：選了日期就刪那天，沒選就刪今天，按鈕文字跟著變。
  確認視窗的家數改成問資料庫，不是用畫面上的列數（同時下了關鍵字篩選時
  兩者不一樣，而刪除是刪整天）。
- **「套用」按鈕與旁邊的輸入框沒對齊**。
- **設定檔路徑那行的說明過時** — 現在總覽可以直接編輯了。

---

## 1.1.0 — 2026-08-05

### 新功能

- **反饋頁**（側邊欄）— 直接在程式裡把問題回報給作者，可以附截圖。
  已設定 Gmail 就直接寄出，還沒設定就開啟你自己的郵件軟體並填好內容。
- **信件附件** — 寄開發信時可以附加檔案，單封信合計上限 20 MB。
  附件庫可以改顯示名稱、寫備註、看用過幾次；被自動排程引用的檔案刪除前會警告。
- **自動排程擴充** — 除了爬取，現在也能排程寄信，或「先爬取再把名單寄出去」。
  新增每月排程（可指定幾號），以及「寄給誰」的篩選條件。
- **公司頁依收集日期檢視** — 下拉選單列出每一天各收集了幾家，
  選定某天後可以整批刪除（某次爬壞時的清理入口）。
- **爬取欄位選擇** — 在「自訂網址」精靈裡勾選這個來源要收集哪些欄位。
- **來源預設產業** — 名錄網站多半是一個分類一頁，分類本身就是產業。
  填在來源上，頁面沒提供產業時自動補上。
- **刪除備份** — 設定頁可以刪掉不要的備份。
- **匯入範例檔** — 下載可以直接填寫的 Excel，附逐欄說明分頁。

### 修正

- **爬取只到第 10 頁就停** — 全域的頁數上限會蓋掉來源自己明確設定的值。
  現在全域值只是「來源沒指定時的預設」，來源說 24 頁就是 24 頁。
- **數字分頁抓不到** — 以前只認得寫著「下一頁」的連結。
  台灣的名錄網站絕大多數是「1 2 3 4 5」這種純數字分頁，
  現在會認出來，並且直接告訴你這個名錄總共有幾頁。
- **公司名稱誤抓成「詳細資料」** — 每張卡片都有的樣板連結命中率是 100%，
  比真正的公司名還高。改用「每一列都不一樣」當主要判準。
- **macOS 介面跑位** — 版面尺寸原本全是照 Windows 字型寫死的。
  改為從字型度量推導，各平台也改用自己的系統介面字。
- **下拉選單文字被截斷**（「電子零組件」變成「電...件」）。
- **控制項沒對齊** — QSpinBox 比其他控制項高 4px。
- **介面顯示完整檔案路徑** — Windows 的路徑含使用者帳號名稱，
  而這些畫面常被截圖分享。改成只顯示相對路徑。
- **缺少的相依套件** — `requirements.txt` 漏了 PySide6 與 cryptography。
  後者缺席時不會當機，只是加密安靜失效。
- **Gmail 帳號狀態顯示兩次「尚未設定」**。
- **「清除」按鈕語意不明** — 改名為「清除篩選」，它只清搜尋條件、不刪資料。

---

## 1.0.0 — 2026-08-03

第一個版本。

- 爬取公開的台灣 B2B 廠商名錄，貼上網址即可自動辨識頁面結構
- 資料清理：正規化、格式驗證、MX 查詢、去重合併
- 公司與聯絡人管理，匯入／匯出 Excel、CSV、JSON
- 用自己的 Gmail 寄送開發信，含樣板、變數、演練模式與多重防護
- 個資欄位 AES-GCM 加密，金鑰存在系統憑證保管庫
- 自動排程爬取、自動備份
