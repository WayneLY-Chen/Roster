# 更新紀錄

## 1.15.0 — 2026-08-11

從展場、名片、同事那裡拿到的 Excel，常常只有兩欄：公司名稱和一支電話。這一版
讓那種名單也補得起來。

### 新增

- **「補齊公司資料」——只有公司名稱也能補**

  「爬取」頁多了第三顆補完按鈕。原本那兩顆各自有前提：「補抓信箱」要先有網址，
  「補公司登記資料」要先有統編。只有名字的名單按哪一顆都會得到「沒有需要處理
  的公司」，而那句話讀起來像是「已經很完整了」——實際上是那兩支都幫不上忙。

  新的這一顆一家公司走三關，每一關都只在真的缺那些欄位時才跑：

  1. **查經濟部商業司**——有統編就用統編，只有名稱就用名稱模糊查詢。補回統一
     編號、登記負責人、登記地址、登記狀態、資本額。
  2. **找官網**——還是沒有網址的話才搜尋。
  3. **抓聯絡資料**——到公司自己的網站上讀公開頁面，帶回信箱、電話、傳真、聯絡人。

  已經有值的欄位一律不會被覆蓋，只補空的。

  命令列是 `python main.py complete`。

- **商業司現在也能用公司名稱查**

  `crawler/registry.py` 原本的註解寫著「名稱查詢在這個資料集上回空陣列」——那句
  話沒錯，但它講的是它用的**那一個**資料集。商工登記公示資料另有一份支援 `like`
  模糊查詢的，只是它有一個不寫在文件裡、少了就一定回空陣列的條件：

  ```
  Company_Name like 台灣積體電路 and Company_Status eq 01   → 1 筆
  Company_Name like 台灣積體電路                            → 0 筆
  ```

  少了狀態條件對方回的是空陣列而不是錯誤，所以看起來會像「這家公司沒登記」。

  模糊查詢會一次回好幾家（「台積電」會回「台積電機」「台積電梯」），所以只有
  正規化後的名稱**完全相同**才算數。八成像不算——補錯一家公司的統編，比留白糟。

- **設定頁多了「搜尋官網（選填）」**

  找官網預設用 DuckDuckGo，不需要申請任何東西。會選它是因為 robots.txt：這支
  程式每次請求前都會查對方的 robots.txt，而 `google.com` 與 `bing.com` 都寫著
  `Disallow: /search`，只有 `html.duckduckgo.com` 是明文的 `Allow: /`。前面那幾個
  不是「我們選擇不用」，是這支程式**自己會把那些網址擋下來**。

  DuckDuckGo 對密集查詢會限流。遇到的話可以在這裡填一把免費金鑰（Brave 每月
  2000 次、Google 每日 100 次），程式會自動改走官方 API。金鑰跟 Gmail 密碼一樣
  存在作業系統的憑證保管庫，不會寫進專案資料夾。

- **「匯入」頁多了「匯入後自動補齊資料」**

  勾了之後，匯入完成會接著對**這一批**公司跑一次補齊，不會動到資料庫裡既有的
  資料。預設關閉，而且應該保持關閉當預設：補齊會連網，匯入 500 筆就是 500 次
  以上的請求，那件事該由使用者按下去，不該是匯入的副作用。單次有家數上限
  （預設 200），超過的部分會照實說明。

- **補抓信箱的同一趟路現在也帶回電話、傳真、聯絡人**

  頁面本來就已經抓下來了，只解析信箱等於白白丟掉另外三個欄位，而多解析幾個
  欄位不會多送任何一次請求。電話與傳真靠標籤區分（「傳真︰」才是傳真），沒有
  標籤時電話退回頁面上任何電話形狀的字串，**傳真則寧可留白**——猜錯的傳真號碼
  會被當成電話撥出去。

### 修正

- **`python main.py security` 在繁體中文 Windows 上直接當掉**

  README 明文要求上傳 git 之前跑一次的那支檢查，在它最主要的使用環境上會以
  `AttributeError` 收場。

  它用 `subprocess.run(text=True)` 跑 `git ls-files`，而 `text=True` 是拿
  **系統語言**的編碼去解碼——繁體中文 Windows 是 cp950。這個專案自己就有
  `docs/名單.md`、`docs/爬取.md`，git 以 UTF-8 吐出來，解碼在 subprocess 自己
  的讀取執行緒裡爆掉。那個例外攔不到，只會讓 `stdout` 變成 `None`。

  改成 `git ls-files -z` 加自己以 UTF-8 解碼，並把這兩個輔助函式搬到新的
  `core/repo.py`。搬家的理由是測試：`main.py` 在 **import 的當下**就會呼叫
  `sys.stdout.reconfigure(...)`，而測試裡的 `sys.stdout` 是 pytest 的擷取
  物件——重新設定它會影響整場測試的輸出處理。實測過差別：測試裡 import
  `main` 的版本，整套跑 6 輪有 4 輪以 access violation 收場。

- **安全檢查看不到「還沒追蹤、但也沒被忽略」的檔案**

  那是風險最高的狀態：還沒進 repo，但離 `git add -A` 只差一步。原本的檢查只
  看「已經被 git 追蹤的檔案」和三個寫死的路徑，中間這一塊是空的。

  實際撞到：一份 2699 家公司的聯絡資料放在專案根目錄，而這支指令印的是
  「✓ 檢查通過，可以安全上傳 git」。現在會擋下來並說明怎麼處理。

- **爬取進度從來沒有真的送到狀態列過（測試看不出來）**

  `_on_crawl_progress` 的第一件事是呼叫 `status_bar.advance_progress(...)`，
  第二件事是 `bump_data_version()`——後者的用意寫在原始碼裡：「爬一趟可能一個
  多小時，公司頁停在畫面上的話要跟著長出來，要等到最後才看得到東西是說不過
  去的」。

  但測試用的 `_FakeApp` 上**沒有** `advance_progress`。於是每一次進度回報都在
  `BackgroundTask._handle_progress` 裡丟 `AttributeError`，第二行永遠不會執行。

  Qt 會把 slot 裡的例外印出來然後繼續跑，所以測試全都是綠的。實際代價有三個：

  1. 進度回報那條路完全沒被驗證過，改壞了也沒人知道。
  2. 「爬到一半公司頁跟著更新」這個功能實際上是壞的——而既有測試的
     `assert current_data_version() == before + 1` 斷言的正好是**壞掉的行為**
     （只有結束時 bump 一次）。
  3. 那個例外是在執行緒池正在送訊號的當下逼 Python 組 traceback，正好命中
     `gui_qt/tasks.py` 開頭記載的那個會讓整個直譯器以 access violation 收場
     的情境——整套測試偶爾會無預警整個當掉，就是這個。

  替身補齊成跟 `gui_qt.widgets.StatusBar` 一樣的簽章，並加了一個守門測試
  逐一比對兩邊的方法簽章，這種漂移不會再無聲發生。

### 「補齊公司資料」實測數字

拿台灣機械工業同業公會的會員名冊（2699 家，只有公司名稱、信箱欄全空）
等距抽 12 家實跑，結果照實列在這裡：

| 欄位 | 命中 |
|---|---|
| 統一編號、登記狀態、登記地址、負責人 | **12/12（100%）** |
| 官網 | 8/12（67%） |
| 電話 | 8/12（67%） |
| 信箱 | **6/12（50%）** |
| 傳真 | 4/12（33%） |

每家約 8 秒。

- **商業司那一關很可靠。** 12/12，而且是官方登記值。只有公司名稱的名單，
  光是這一關就補得回統編、負責人與登記地址。

- **留白的 4 家是刻意留白的。** 其中 3 家搜尋到的是公司資料聚合站，1 家的
  官網對 `robots.txt` 回 403（依這支程式一貫的保守政策視為整站不允許）。
  第一版沒擋掉那 3 個聚合站，結果之一是某家公司的「官網」抓成一家展覽公司
  的型錄頁，**信箱跟著抓成那家展覽公司的 `info@`**——一個錯的信箱進到寄信
  名單，比缺一個信箱糟得多。

- **抓到的 8 個網址全是公司自己的網域**，而 6 個信箱裡有 5 個跟官網同網域
  ——那是「這個信箱是對的」最強的訊號。

- **它仍然只是「最可能的那一個」，不是保證。** 完全不想要這種不確定性的話，
  把 `config.yaml` 的 `completion.search_provider` 設成 `none`，就只補商業司
  那一關與「檔案裡本來就有網址」的公司，那兩者都是確定的。

- 大公司比中小企業難很多：另外拿台積電、大同、信邦跑過，三家的統編全中，
  但信箱 0/3——大公司的聯絡方式幾乎都藏在表單後面，不是明文刊登的，那種
  這支程式不會去碰。

## 1.14.1 — 2026-08-06

### 修正

- **按「＋ 自訂網址…」會當掉**

  一按就是 `AttributeError: 'bool' object has no attribute 'get'`，精靈打不開。

  Qt 的 `clicked` 完整簽章是 `clicked(bool checked = false)`——它會把那個布林值
  當成第一個參數送給接收的函式。而那個函式的第一個參數是「要編輯哪一個既有
  來源」，於是「開一個新的」變成「編輯 `False` 這個來源」。

  漏掉的原因很明確：原本的測試是**直接呼叫**那個函式，不是真的按下按鈕——直接
  呼叫時參數是對的，怎麼測都不會出事。現在的測試改成真的發出訊號。

  順便把介面裡所有接到訊號的函式掃過一遍，確認沒有第二處同樣的誤會。

- **macOS 的 App 點下去沒反應時，現在至少查得到原因**

  以前只有「Python 有跑起來但失敗」才會留下紀錄。真正難查的是**更早**就掛掉的
  狀況——那時候畫面上什麼都沒有，磁碟上也什麼都沒有，等於無從查起。

  現在每一次啟動都會先在 `logs/啟動失敗.log` 寫一行時間。有那一行代表 App 的
  外殼有跑到，沒有代表問題在更外層（執行權限、下載隔離標記、bundle 結構）。
  錯誤對話框開不起來時（系統政策擋掉 osascript）會改成直接把紀錄檔打開。

- **建立 App 時會檢查專案路徑真的寫進去了**

  寫入路徑有兩條路（`python3`，失敗才退回 `sed`）。兩條都失敗的話會產生一個
  還寫著佔位字串的啟動器——檔案在、也可以執行，所以原本的檢查過得了關，但雙擊
  時它會去找一個叫 `__PROJECT__` 的資料夾。使用者看到的是「App 建好了，可是
  點不開」，而這正是最難自己查出來的那一種。

## 1.14.0 — 2026-08-06

有一類名錄的清單上只有公司名稱，電話、信箱、負責人全在「點一下跳出來的那一塊」
裡面。以前爬回來每一筆都只有名字——看起來有資料，實際上全是空的。這一版讓分析
自己把這種網站認出來、並且把方法設定好。

### 新增

- **分析會自己認出「詳細資料是點出來的小視窗」，並且設定好**

  分析多了一步：點開第一筆看看會不會跳出一塊東西。判斷方式是「點之前看不見、
  點之後看得見」，而不是「頁面上有沒有 class 帶 modal 的東西」——那種容器在很多
  網站上一直都在（藏著），光看存不存在會把 cookie 提示、登入視窗都認成明細。

  認出來之後還要確認**裡面真的是聯絡資料**：用讀名錄那一套去拆，至少要拆得出
  兩個看得懂的欄位。少了這一關，一個純圖片的廣告彈窗也會被存成明細設定。

  連「怎麼關掉它」也一起找。關不掉的話，下一列點下去會被前一個蓋住，第 2 筆
  之後全部讀到同一份內容，而且看起來完全正常。

- **附件可以開啟，也可以另存到別的地方**

  以前附件加進去之後只剩「刪除」，等於進了一個看得到、打不開的地方——要看內容
  只能自己去翻資料夾，而那個資料夾在哪裡程式也不會告訴你。

- **信件內文可以調字級**

  工具列多了一個字級下拉，單位是 px（跟 Gmail 一樣）。用 px 不用 pt，是因為信
  最後是在網頁郵件裡被讀的，選幾就是幾，中間沒有換算。下拉會跟著游標走：點到
  一段 24px 的字，選單就停在 24 px。

- **安裝畫面上有一隻貓跟一隻狗**（Windows / macOS / Linux 都有）

### 變更

- **錯誤訊息改成中文，而且關得掉**

  以前資料庫的錯誤會把**整句 SQL 連同所有參數**印在畫面上，其中包含加密後的
  欄位——使用者看到的是一段英文加一串密文，看不懂、不知道要做什麼，截圖一貼
  出去就外流了。現在講的是「這家公司底下已經有同名的聯絡人了」這種話，原始
  內容留在日誌裡。訊息右邊多了一個 ✕：以前那句話一旦出現就佔著位置不走，
  問題修好了還在，看起來像又錯了一次。

- **分析會把清單選擇器收斂到「真正是廠商的那幾列」。** 挑清單靠的是「哪一種
  區塊重複最多」，挑到的常常是 `tr` 這種很寬的東西，同一頁上的子分類表、表頭
  也一起被框進去。現在會拿「真的點得開小視窗的那一列」往上找，只框住它所在的
  那一張表。

### 修正

- **往下點一層的名錄，只有第一個子分類抓得到詳細資料**

  點完第一列之後畫面已經換成那個子分類底下的廠商清單了，程式卻還留在那一頁去
  點「第 2 列」——點到的是**廠商**，開出來的是明細小視窗，不是下一個子分類。
  結果是第 2 批之後每一筆都是同一家公司，而且一個欄位都沒有。現在每一列都會
  先把查詢重做一次再點下去。

- **交出去的網頁內容與詳細資料是頁面在不同時刻的樣子。** 詳細資料是靠**位置**
  對回每一列的，而點開小視窗會動到頁面——先收集再讀網頁的話，第 2 筆的電話會
  掛到第 5 筆頭上，或者整批對不上而全部落空。現在網頁內容一定先讀。

  讀到一半清單被換掉時會停手，其餘留空——少一點資料，比錯的資料好。

- **活動記錄的「新增」沒有跟輸入框對齊。** 旁邊的「類型」與「主旨」都是兩行高
  （說明文字在上、控制項在下），而按鈕只有一行，會被擺在那一列的垂直中央，
  看起來浮在上面一截。

## 1.13.0 — 2026-08-06

### 新增

- **可以編輯已經存好的爬取來源**

  「管理自訂來源」多了「**編輯選取的來源**」。以前存好的來源只能看跟刪——想改
  一個選擇器、或改「要爬哪一段」，唯一的辦法是整個刪掉、重貼網址、重新分析
  一次；而分析要一兩分鐘，還會把你自己調過的每一格覆寫回偵測結果。

  現在會把設定檔裡那一份原樣填回精靈的每一格：網址、名稱、選擇器、頁碼範圍、
  要收集哪些欄位、要不要讀檔案、逐項查詢的範圍與「中間再點一層」，連「這個
  網站要用瀏覽器才爬得到」也一起帶回來。**不會重新連線到那個網站**——你要改的
  是自己當初的決定，那些設定檔裡都有。要重驗的話「分析網頁」那顆按鈕仍然在。

  改名字的話舊的那一份會一併收掉，不會留下一個沒有人想要、排程卻照樣去爬的
  孤兒來源。

### 變更

- **被丟掉的資料現在會講出是哪幾筆。** 上一版只在「一筆都沒留下來」時才說話，
  10 筆丟 3 筆是靜悄悄的——而那其實更常見。現在只要有東西被丟掉就會列出
  那幾筆的公司名稱欄實際抓到什麼文字（最多五筆）。一筆都沒留下來跟只丟幾筆
  的講法與嚴重度也分開了：前者代表整個抓錯位置，後者常常是正常的。

## 1.12.0 — 2026-08-06

這一版是修東西的。有一個網站抓回來 21 筆、一筆都沒存進去，而畫面上看不出
哪裡不對——追下去發現不只一個問題。

### 修正

- **分析交出來的設定會自相矛盾，導致整趟爬取一筆都存不進去**

  同一頁上有下拉選單與關鍵字兩個查詢框時，程式兩條都會試。選單那條查出來
  是商品分類、往下點一層只找到一家公司（低於「至少 3 筆」的門檻）而被判定
  失敗，改用關鍵字那條成功——但收尾時無條件把查詢表單還原成選單那條的，
  讓選單能用的「再點一層」設定也一起丟了。

  存出來的來源於是**用選單查、卻套著關鍵字學到的欄位**，爬回來全是分類代號，
  一筆都不是公司。

  欄位長什麼樣是「結果表」的性質，不是「走哪一條路」的性質——兩條路最後
  渲染的是同一張表。現在關鍵字那條走通之後會回頭再確認一次選單，而且會記下
  真正驗證過走得通的是哪一條；選單那條試過走不通時，精靈會直接說出來，不再
  提供一個按了會白跑的選項。

- **逐項查詢被「最多爬幾頁」壓住**

  兩個欄位都在講「總共跑幾趟」，取最小的結果是使用者沒去動的那個（預設 3）
  默默贏了——選單那格填了 97，實際查到第 3 個就結束，畫面上還寫著完成。
  現在逐項查詢的趟數由它自己那一格決定，勾了逐項查詢時頁數欄位會收起來。

- **抓到東西卻一筆都存不進去時，畫面上只有三個數字**

  「找到 21、新增 0、拒絕 21」擺在一起沒有人看得出那是壞的。現在會直接說
  「全部都不是公司資料」，並把實際抓到的文字印出來——一眼就看得出抓錯位置。

- **「取消」做的其實是「暫停」**

  它會記住進度、下一次自動接著跑。一顆寫著取消的按鈕做的是暫停，那是標籤
  在說謊。拆成兩顆：暫停留著進度，取消把進度清掉。

- **macOS 的 Roster.app 點下去直接消失**

  Finder 啟動 .app 時沒有終端機可以印東西，舊的啟動器一失敗就靜靜結束——
  圖示跳一下就不見，真正的原因（資料夾搬過位置、環境沒裝好）只留在沒有人
  會去看的系統紀錄裡。現在會用對話框把原因說出來，並寫進 `logs/啟動失敗.log`。

### 新增

- **逐項查詢可以指定要爬哪一段。** 97 個分類跑完要好幾個小時，本來就會分次跑。
  兩種指法擇一（選了一種另一種反灰）：用序號「從第 7 個到第 9 個」，或用
  選項上的文字「從 03 魚類 到 10 動植物油脂」。文字對不到任何選項時會退回
  序號並在日誌裡說明，不會安靜地爬成完全不同的一段。

- **分析網頁看得到跑到哪一步。** 以前是一條來回跑的橫槓，而分析一個 JavaScript
  名錄要一兩分鐘。現在走固定的六個階段，第幾步、共幾步、這一步在做什麼都寫出來。

- **爬取有真的進度條**（第幾趟／共幾趟），不再是來回跑的橫槓。

### 變更

- 公司頁停在畫面上時會自己長出來，不用切頁——背景每存一批就更新一次。
  爬一趟可能一個多小時，要等到最後才看得到東西是說不過去的。
- 分析網頁時整張表單反灰（改到的東西幾秒後會被分析結果覆寫掉），但精靈
  不再擋住主程式，也可以縮到旁邊去。
- 儲存區直接寫明：上面的勾選會在按「儲存」的當下一起存進去，不用再按一次
  「分析網頁」。

## 1.11.0 — 2026-08-06

一份剛爬回來的名單，每一列的可用程度差很多，而且裡面一定混著早就倒掉的公司。
這一版處理的就是這兩件事：**先聯絡哪一批**，以及**哪幾家已經不在了**。

### 新增

- **名單品質分數**

  每一家公司多了一個 0 到 100 的分數，公司列表上新增「品質」欄，篩選列右邊
  多了「排序」下拉，預設把有信箱、有電話、資本額大、資料新的排在前面。

  配分看得到：打開一家公司的詳細資料，「名單品質」那一區會把分數怎麼來的
  攤開寫（信箱 40、電話 20、資本額 15、資料新舊 10，其餘是網站、聯絡人、
  地址、統編），知道要往上拉的話下一步該補什麼。

  兩種情況會被壓到最低：登記狀態已經是解散／撤銷／廢止的，以及標記過
  「不再聯絡」的——資料再完整都不該排在前面。

  分數是每次查詢時算的，不存進資料庫。裡面有一項是「資料多新」，存成欄位的話
  今天算的分數明天就不對了，而且沒有任何地方會知道它不對。

- **用統一編號補公司登記資料**

  「爬取」頁多了一顆「補公司登記資料」。它會拿名單裡的統一編號去查經濟部
  商業司的商工登記公示資料，補上登記狀態、資本額，以及還空著的登記負責人
  與地址；登記名稱、實收資本額、設立日期、登記機關放進公司詳細資料的
  「其他欄位」。

  最有用的其實不是資本額，是**登記狀態**。名錄網站不會把倒掉的會員刪掉，
  所以一份名單裡混著解散、撤銷、廢止的公司。跑完之後公司列表多一欄看得到，
  活動紀錄也會直接寫「其中 N 家的登記狀態已經不是核准設立」。

  只能用統一編號查——這個資料集不支援用名稱查詢，所以沒有統編的公司這一步
  幫不上忙，程式會照實說「沒有統一編號的查不了」，不會拿名稱去猜。對方
  尖峰時間會忙線，遇到就跳過那一筆而且**不**標記成已查過，下一次自然會再試。
  查過的半年內不會重複查。

  資料適用政府資料開放授權條款－第 1 版，允許商業利用，但顯名標示是強制
  義務——「未盡顯名標示義務者，視為自始未取得授權」。所以來源標示會固定
  出現在設定頁、公司詳細資料，以及**帶有登記資料的每一份匯出檔**（Excel 多
  一張「資料來源」工作表，CSV 補一列，JSON 多一個欄位）。沒用到登記資料的
  檔案不會多這些東西。

### 變更

- 匯出的預設欄位加入「名單品質」「登記狀態」「資本額」。

- 說明文件重新整理。README 從 723 行縮到 257 行，只留安裝、五分鐘上手、
  每一頁在做什麼與導覽；爬取技巧、名單整理、寄開發信、自動排程各自搬到
  `docs/` 底下一個檔案。要找一件事不必再從頭捲到尾。

## 1.10.0 — 2026-08-06

這一版沒有新的爬取能力，處理的是兩件更基本的事：**壞掉的時候要看得見**，
以及**跑到一半斷掉不要白做工**。

### 新增

- **「這次抓到的比上次少很多」提醒**

  整套爬取建立在「程式該去網頁哪個位置取值」上。網站一改版那個位置就失效，
  而爬取仍然會「成功」——只是一筆都沒抓到，畫面上還寫著完成。這是最難發現的
  一種壞掉，尤其排程是半夜自己跑的，沒有人在看。

  現在會拿這一次跟**上一次真的有抓到東西**的那一次比：抓到 0 筆、或掉到不足
  四成，就在結果表格的新欄位「提醒」與活動紀錄裡講出來，狀態列也會變成提醒色。

  門檻刻意寬鬆（四成）。名錄本來就會增減，每次都跳警告等於沒有警告；要攔的是
  0 筆、以及「240 筆變 12 筆」那種斷崖。失敗、取消、以及接續上次的執行都不會
  再多報一次——那些本來就有自己的說明。

- **中途斷掉可以接著跑**

  逐項查詢一趟可能好幾個小時。以前第 80 個條件時網路斷一下，下次得從第 1 個
  重來。現在每一批資料存進資料庫的**同一個交易**裡會一起記下進度，下一次自動
  接續，活動紀錄會寫明這一次是接續的。

  進度只在一個查詢條件底下的東西**全部**做完之後才前進——中間還要再點一層的
  網站，不這樣做會把還沒點完的那一個整個跳過。跑完就把進度清掉，否則下一次會
  從中間開始，前面那幾頁永遠抓不到。

  只有「網址帶頁碼」的分頁與逐項查詢接得回去；靠「下一頁」連結走的名錄沒有
  可定址的頁碼，跳不過去。

### 變更

- 一次加入好幾個名錄時，整批共用同一個瀏覽器。以前每一個網址各開一次
  Chromium，光是啟動就 2～4 秒，十個就是半分多鐘純粹在等——而且開十次跟開
  一次拿到的結果一模一樣。
- 狀態列多了「提醒」色。跟錯誤的紅色分開：爬取本身是成功的，用紅色會讓人
  以為壞掉。

## 1.9.0 — 2026-08-06

### 新增

- **中間還要再點一層的名錄**

  有些網站是三層的：選一個大分類 → 出來一張**子分類**清單 → 點其中一項才看得到
  廠商。中間那一層看起來很像資料（整齊、有連結、筆數也對），裡面卻一家公司都
  沒有。少了這一步，跑完幾十次查詢的結果會是「一筆都沒抓到」，而且完全不會
  報錯。

  逐項查詢現在會逐列往下點，每一個狀態各收一次。分析時也會自己試著點下去，
  **確認點下去真的會出現廠商才採用**——中間那一層本來就長得像資料，光看是分
  不出來的。

  每一列都是一次往返，所以有硬上限（預設每輪 20 列）。這既是時間的問題，也是
  對別人網站的基本禮貌。

### 修正

- `macOS/_共用.sh` 在版控裡少了執行權限。這是專案自己的檢查抓到的：上一版
  提交時它還是未追蹤檔案，掃描範圍剛好跳過它。

## 1.8.0 — 2026-08-05

這一版的主題是「不必再為了某一種網站改程式」。以前遇到一種新的網站寫法就要
補一種對應；現在退一步：看不到資料就開真的瀏覽器再看一次，該填的欄位就填、
該按的按鈕就按。後端是 PHP、ASP.NET 還是回 JSON 的介面都一樣。

### 新增

- **看不到資料時自動改用內建瀏覽器重看**

  現在有一大半的名錄是「網頁先送空殼，再用程式把資料填上去」，直接讀原始碼
  一個字都看不到。分析時判斷抓到的東西不像廠商名錄，就自動再開一次瀏覽器
  重新分析，而且**把這件事記在這個來源上**——之後每次爬取都會用同一種方式，
  使用者不必知道也不必去改任何設定。

  引擎從「全域一個設定」改成**每個來源各自決定**。以前為了一個網站要把全域
  設定改成瀏覽器，等於所有靜態名錄也跟著慢好幾倍。

- **逐項查詢：只給查詢框、不給完整清單的名錄**

  有一類名錄不給你「全部列出來」，只給一個查詢框：選一個分類、或打一個關鍵字，
  按下查詢才會出現廠商。那種頁面沒有第 2 頁可以翻，把條件逐一查過去就是它的
  「翻頁」。

  分析時會**數出這個選單有幾個條件**（跟偵測總頁數同一個意思）並顯示出來，
  勾了才會執行，預設只查前 3 個——先少量試跑，確定抓對了再放大。打關鍵字
  查詢的網站則自己填要查哪些字。

  分析遇到這種頁面時會**自動試查一次**，用查詢結果推出欄位——還沒查詢的頁面
  上一筆資料都沒有，只看那一頁是猜不出「一筆資料長什麼樣」的。

- **點出來的小視窗裡的詳細資料**

  另一種明細：不是連到另一個網址，而是點一下在同一頁彈出來。清單上通常只有
  公司名稱、負責人、地址，電話與信箱全在那個小視窗裡。現在會逐列點開、讀完
  再關掉，內容按位置一一對應回去；點不開的那一筆留空，絕不讓某一筆撿到別人
  家的電話。

- **「找名錄…」可以複選**

  結果視窗多了勾選欄與「全選」「全部取消」。一個網站常常有好幾份名錄（各縣市
  分會、各產業分類），一次只能挑一個等於同一套流程要重跑五遍。勾多個時會逐一
  分析並各存成一個來源，抓不到公司名稱的會跳過並說明原因。

  只有最像的那一個預先勾起來——全部預勾等於幫使用者做了他沒說要做的決定。

- **安裝程式會下載內建瀏覽器**（Windows／macOS／Linux 三個都有）

  Chromium 不是 pip 套件，放不進 requirements.txt。這一步失敗不是致命的，
  一般的網站沒有它照樣爬得到。

### 變更

- 分析中或站內尋找中，網址欄位會鎖起來不給改。改了也不會影響正在跑的那一次，
  但畫面顯示新網址、結果卻是舊網址的，比直接不給改難查得多。
- 分析的等待訊息會先講「可能會多花半分鐘」，而不是讓人盯著不動的進度條。
- macOS 的 `.command` 跑完會自己關掉終端機視窗，不再留下一個寫著「[程序完成]」
  的空視窗。比對的是 tty，不會動到其他終端機視窗。
- 「找名錄…」結果視窗的型態欄補上 PowerPoint。

### 修正

- **macOS 建立 Roster.app 失敗**：`app?: unbound variable`。變數後面直接接
  全形括號時，bash 會把括號的第一個位元組當成變數名的一部分。App 其實已經
  建好了，只有最後那行訊息炸掉，卻讓安裝程式回報「建立失敗」。
- **斑馬紋表格被切成兩半**：一列一列輪流加的 `odd`／`even` class 讓 20 列的
  表格被當成兩組各 10 列，分數砍半——同一頁上 8 個項目的導覽選單反而贏過真正
  的廠商資料，分析回報「找到 8 筆」，內容是「加入會員、找商機、認識公會」。
- **下拉選單的選項被當成資料**：一個有 98 個商品分類的查詢選單重複度完美，
  分數輕鬆壓過同一頁上真正的 20 列廠商。`<option>`、`<script>` 這一類永遠
  不是「一筆資料」。
- **導覽選單被當成名錄**：清單評分現在把「內容有多少比例真的像公司名」算進去。
  結構上導覽選單跟名錄一樣整齊，只有內容分得出來。
- **「連絡電話」抓不到**：欄位對照表只有「聯絡電話」。台灣的名錄兩種寫法都有
  人用，少列一種的代價是那些站的電話整欄空白。傳真同樣補上。
- **`(02 )2740-7278` 這種電話抓不到**：區碼括號裡有空格是資料庫欄位補空白直接
  印出來的結果，在公協會名錄裡很常見。
- **日誌一打開橫向捲軸就在中間**：捲到最後一行是對的，但游標同時被帶到那一行
  的行尾。時間與分類在行首，那才是要先看到的東西。
- 查詢按鈕的選擇器現在保證只指到一顆。有分頁籤的查詢頁有好幾顆長得一模一樣的
  查詢鈕，挑到錯的那一顆按下去什麼都不會發生，而且不會報錯。
- 彈出視窗裡的「確定」不會再被當成查詢按鈕。

## 1.7.0 — 2026-08-05

這一版處理三件「資料明明看得到、程式卻抓不到」的事：名錄藏在網站深處、名冊
是一個 PDF、電話藏在按鈕後面。

### 新增

- **PDF／Word／Excel／PowerPoint 檔案裡的名單**

  不少公協會沒有把會員名冊做成網頁，而是掛一個 PDF 或 Excel，常常還要先點進
  某個子頁面才看得到那個連結。現在爬取與「找名錄…」都會跟進去把裡面的名單
  讀出來。

  **分析網頁時會實際去看這一頁連出去幾個檔案**，並且只讓真的有的格式勾得動
  ——勾了不會發生任何事的選項比沒有那個選項更讓人困惑。位置在精靈的
  「要收集哪些欄位」旁邊，**預設全部不勾**：讀別人的檔案跟讀網頁不是同一件事，
  該由使用者決定。

  表格（Excel、PDF 裡有格線的表）走匯入那條路，欄位對應與「對應不到的欄位
  原樣保留」跟匯入完全一致；純文字（Word、沒有格線的 PDF）走「負責人︰王大明」
  的標籤解析。

  **讀出來不像廠商名冊的檔案會整份略過**——章程、會議記錄、年報都落在這一類。
  一份年報要是照單全收會產生幾百筆垃圾，混進資料庫之後很難分辨。

- **點了才看得到的資料**

  有一類名錄把電話與信箱藏在「顯示電話」按鈕後面，或只先載入前 20 筆、要按
  「載入更多」才出現其餘的。那些資料在原始網頁裡根本不存在。

  分析時偵測到這種按鈕，精靈裡的「先點開頁面上的按鈕」就會啟用，勾了之後
  爬取會自動點開它們。支援按一次、每一列各按一次、連續下捲、單純等待四種。
  需要把設定裡的爬取引擎改成 `playwright`；用 httpx 卻設了動作時會明確警告，
  不會安靜地忽略。

- **「找名錄…」：給一個網站，程式自己找出名錄在哪一頁**

  以前必須先自己找到名錄那一頁才能貼進來。但名錄常常藏在「關於我們 → 組織
  → 會員專區」底下三層，從首頁完全看不出來——找那一頁往往才是麻煩的部分。

  「＋ 自訂網址…」多了一顆「**找名錄…**」：貼上這個網站的任一個網址（首頁
  就可以），程式會在站內走訪，把看起來像廠商名錄的頁面列出來，附上每頁幾筆、
  共幾頁、以及**實際抓到的前幾個名稱**。選一個，精靈就接著分析它。

  判斷依據是內容而不是版面：一個頁面就算有整齊的重複結構、也抓得到信箱，
  只要抓出來的是問答標題而不是公司名稱，就不會被當成名錄。

  對別人的伺服器：讀取頁數有硬上限（預設 30 頁），按下去之前先告訴你要讀幾頁、
  預估多久；網站有 sitemap 就優先照著看；同一個名錄的第 2、3 頁不重複分析；
  robots.txt 與請求間隔延遲跟一般爬取完全一樣。

- **整個介面的文字寬度自動檢查**（測試）— 在 10／13／16pt 三種字級下把每一個
  按鈕、標籤、下拉選單都量一次，確認文字放得下自己的框。

### 變更

- **側邊欄寬度改為跟著字寬算**（原本寫死 210px）。
- **下拉選單的寬度上限改為跟著字寬算**（原本寫死 240px）。
- **匯入不再把對應不到的欄位丟掉**，改為原樣保留（跟檔案解析同一套規則）。

### 修正

- **macOS 的下拉選單文字被切掉** — 「（全部啟用）」顯示成「（全部啟用」。
  原生樣式的下拉箭頭區比 Qt 的預估值寬，全形括號又比字寬估計值再寬一些，
  加起來差的那幾個 px 就吃掉最後一個字。
- **有意義的 class 被當成排版用的濾掉** — 判斷「這個 class 是不是 Bootstrap
  那類排版工具」的規則太寬，`more`、`main`、`date`、`post` 全部中招。後果是
  「載入更多」這種按鈕的選擇器退化成一個光禿禿的 `button`，在整頁比對到
  幾十個元素。
- **背景執行緒踩到已經釋放的資料庫連線** — 背景工作用的是全域執行緒池，
  它活得比任何一次操作都久。前一件工作還沒跑完就把資料庫連線關掉，那條
  執行緒再送一次查詢就是在用已經釋放的記憶體，整個程式直接消失、沒有任何
  錯誤訊息。現在會先等背景工作收工，再拆掉連線。

---

## 1.6.2 — 2026-08-05

### 新增

- **`macOS/診斷.command`** — 把系統版本、Python、虛擬環境、檔案權限、隔離標記、
  以及重新建立 `Roster.app` 的完整輸出一次列出來。裝不起來時雙擊它，把輸出貼
  出來就能定位問題。

### 變更

- **安裝程式不再吞掉 `Roster.app` 的建立錯誤** — 原本畫面只寫「建立失敗」，
  看不出是什麼原因。現在會把完整錯誤印出來。
- **`建立App.command` 產生完會自己驗證一次** — 不再有「畫面寫已建立、實際是
  空殼」的情況。
- 「開不起來 請先看這個.txt」補上終端機視窗不自動關閉的設定位置。

### 修正

- **一個會讓 `建立App.command` 靜悄悄中止的寫法** — `sed | head` 在
  `set -o pipefail` 之下可能因為 SIGPIPE 讓整條管線算成失敗。

---

## 1.6.1 — 2026-08-05

雙擊「安裝.command」會出現「Apple 無法驗證是否為惡意軟體」，而且**按右鍵
「打開」也沒有用**——macOS 15 (Sequoia) 把那個繞法拿掉了。

### 新增

- **`macOS/開不起來 請先看這個.txt`** — 寫著怎麼讓安裝程式跑起來的第一次
  （一行終端機指令，或改用 `git clone` 從一開始就避開）。

### 變更

- **安裝程式執行完會自動解除整個資料夾的「下載自網路」隔離標記**，之後啟動
  就不會再被擋。只動這個資料夾，不改任何系統設定。
- **`Roster.app` 的版本號改成從程式本身讀**（原本寫死在腳本裡，停在 1.3.0）。
- README 原本寫「按右鍵 →『打開』」，那在新版 macOS 上是錯的，已改掉。

### 修正

上一版把啟動檔分到各自的作業系統資料夾時，`安裝.command` 裡的檔名沒有跟著改：

- **`Roster.app` 從來沒有被建立過** — 它去執行一個已經不存在的檔案，而失敗
  是被吃掉的，畫面只會顯示「建立失敗，用 start.command 也可以」，而
  `start.command` 也已經不叫這個名字了。
- 安裝完成的說明寫著錯誤的檔名。

---

## 1.6.0 — 2026-08-05

這一版是為了一類爬不動的網站：2000 年代的公會名錄。它們整頁只有表格排版、
沒有一個 CSS class、內容是 Big5，欄位以「負責人︰王大明」這種文字形式列出。
以選擇器為主的分析對它們幾乎完全無效，程式只會回報「找不到公司名稱」。

### 新增

- **其他欄位（自由欄位）**

  每個名錄有什麼欄位是那個公會決定的，不是程式能事先列完的。旅行公會列
  「會員代表」「入會年月日」「註冊編號」，化工公會列「代理廠商及代銷產品」。
  以前這些會被丟掉——使用者在網頁上看得到，在程式裡卻找不到。

  現在抓到什麼就留什麼，照名錄上原本的名稱存起來：公司「詳細資料」裡多了
  「**其他欄位**」一區，看得到、能改，也能自己新增。

- **「標籤︰值」排版的解析** — `負責人︰`、`Tel︰`、`統一編號︰`、`營業範圍︰`
  這些寫在文字裡的欄位現在讀得出來了。認得出的標籤自動填進對應欄位
  （負責人→聯絡人、FAX→傳真），認不出的就成為上面的自由欄位。

  這件事沒有辦法用 CSS 選擇器做——那種頁面裡「負責人」跟它的值只是同一格裡
  的兩段文字，沒有任何標記分得開它們。

- **頁面自己宣告的編碼會自動採用**（Big5 站台不再整頁亂碼）。

### 變更

- **匯出時每個自由欄位各佔一欄**。
- **匯入時對應不到固定欄位的欄也收得回來**，不再直接丟掉。
- **公司詳細資料視窗改為可捲動** — 欄位數量現在由名錄決定，不再是固定的。

### 修正

- **Big5 網站整頁變亂碼** — 這些站的 HTTP 標頭常常不附編碼，只寫在頁面自己
  的 `<meta>` 裡。少了它，中文會被當成 UTF-8 解讀而變成亂碼——而亂碼的公司
  名稱看起來仍然「有值」，不會報錯，只會安靜地存進一堆看不懂的字。現在改讀
  頁面自己的宣告。
- **一筆資料被拆成好幾筆** — 判斷「要不要往結構更裡面找」的條件太寬鬆，
  會把一張公司卡片拆成它裡面的每一個連結、或把一筆紀錄拆成它的每一列。
  現在會先確認拆出來的東西本身仍然像一家公司。
- **沒有標題與 class 的頁面找不到公司名稱** — 舊式名錄的公司名稱就是卡片裡
  的第一段文字，而原本的判斷只看標題標籤、連結與 class 名稱。
- **清單選擇器選到排版用的表格** — `table` 這種只有標籤名的選擇器存起來之後
  會連版面表格一起選中。現在會自動加上範圍限定，並驗證加了之後選到的東西
  跟偵測時完全一致。
- **地址被當成公司名稱** — 「…中山北路二段45號」結尾的「號」也是小型商家
  名稱的常見結尾（「祥發包裝材料行」那一類），沒有分開處理的話，一個沒有
  公司名稱的頁面會拿地址頂替。
- **網址抓到別家公司的** — 有名錄在公司沒有網址時，直接把上一家的連結留在
  原地、只把顯示文字清空。照連結位址抓的話，一整串沒有網站的公司會被填上
  別人的網址，而畫面上根本看不到那個網址。現在以顯示文字為準。
- **全站共用的值不再被當成公司資料** — 信箱、電話、傳真、網址、統編這幾項
  如果每一筆抓到的都一樣，那不是這家公司的資料。
- **詳細資料視窗可以捲動** — 欄位數量現在由名錄決定，不再是固定的。

---

## 1.5.0 — 2026-08-05

### 雙擊安裝

以前第一次使用一定要開終端機打指令建虛擬環境。現在打開你的作業系統對應的
資料夾，雙擊「安裝」就好——它會檢查 Python 版本、建立虛擬環境、安裝套件，
macOS 還會順便產生 `Roster.app`。可以重複執行，已經裝好的部分會跳過。

找不到合適的 Python 時會直接告訴你去哪裡下載，而不是丟一串錯誤訊息。

### 依作業系統分資料夾

啟動檔原本全部堆在根目錄，使用者得自己分辨哪個是自己該點的。現在分成三個
資料夾，打開就知道要用哪一個：

```
Windows/   安裝.bat   啟動.bat   命令列.bat
macOS/     安裝.command   啟動.command   命令列.command   建立App.command
Linux/     install.sh   start.sh   console.sh
```

根目錄也順手整理過：打包相關的檔案移到 `packaging/`。
其餘留在根目錄的都是被慣例釘住的——`requirements.txt` 是 pip 找的位置、
`README` 與 `LICENSE` 是 GitHub 只認根目錄、`.gitignore` 是 git 只認根目錄。

---

## 1.4.0 — 2026-08-05

### 修正：每個對話框都會開兩次

點兩下開啟公司詳細資料，關掉之後它又自己開一次——每一個用雙擊開啟的視窗
都是這樣。原因是表格同時接了 `doubleClicked` 與 `activated` 兩個訊號，而
**一次雙擊會同時送出這兩個**，處理函式因此被呼叫兩次。

### macOS 可以雙擊啟動了

macOS 的 Finder 不會執行 `.sh`，它只會用文字編輯器打開——所以之前只能開
終端機打指令。現在有兩個選擇：

| 檔案 | 用途 |
|---|---|
| `start.command` | 雙擊直接啟動（會跟著開一個終端機視窗） |
| `./make_macos_app.sh` | 產生 `Roster.app`，可以放進「應用程式」或 Dock，不會開終端機 |

第一次開啟若被 Gatekeeper 擋下來，按右鍵 →「打開」即可。

### 其他

- **公司詳細資料視窗被壓扁** — 新增三個欄位之後變成 12 列，原本寫死的
  高度會把每一列的文字切掉一半。改成跟著字型度量算。
- **應用程式密碼的網址變成可點的超連結** — 要使用者自己把一長串網址抄進
  瀏覽器，是設定流程裡最容易打錯也最沒必要的一步。

---

## 1.3.0 — 2026-08-05

### 新增三個公司欄位

台灣的公協會名錄普遍會列這些，但程式原本沒有地方存：

| 欄位 | 說明 |
|---|---|
| **英文名稱** | 做外銷或對帳時要用 |
| **傳真** | 名錄上跟電話並列，很多老廠商仍在用 |
| **主要產品／代理品項** | 名錄上的「代理廠商及代銷產品」 |

三個都可以爬取、匯入、匯出、在詳細資料視窗裡編輯，也可以在信件樣板裡當
變數用（`{english_name}`、`{products}`）。

### 其他

- **公司列表加上「聯絡人」欄**。
- **空欄位不再留白** — 列表顯示「—」，詳細資料視窗顯示「（無資料）」。
  留白讓人分不出「這家沒有這項資料」與「程式沒抓到／還沒載入」。
- 詳細資料視窗重新排版，把同一類的欄位放在一起
  （公司名稱／英文名稱、電話／傳真、網址／地址）。

---

## 1.2.0 — 2026-08-05

### 新功能

- **更新資訊頁**（側邊欄）— 就是你正在看的這一頁。
- **在設定總覽直接改設定** — 點兩下或按右鍵編輯爬取引擎、爬取延遲、
  頁數上限預設值、信箱 MX 驗證、從 Gmail 收件匣抓聯絡人。
  其餘項目維持唯讀：「遵守 robots.txt」關掉會讓程式做出不該做的事，
  留在設定檔裡讓要改的人自己去看那段說明；加密切換會重寫整個資料庫。
- **單日資料匯出**（設定 → 備份）— 把某一天收集到的公司單獨存成
  Excel／CSV／JSON。這是匯出不是備份：產生的檔案可以重新匯入或交給別人，
  但沒辦法拿來還原整個資料庫（SQLite 的備份是整個檔案複製，
  「只備份某一天」還原回去會把其他日期的資料一起抹掉）。

### 變更

- **不再預先附上任何爬取來源** — 來源清單預設是空的。
  要爬哪些網站是你自己的決定，程式不預先塞範例目標進去。
  用「爬取」頁的「＋ 自訂網址…」貼上網址即可新增。
- **「每個來源頁數上限」改名為「頁數上限預設值」** — 那個值現在只是
  「來源沒指定時的預設」，原本的名稱正好是相反的意思。
- **「Gmail 讀信」改名為「從 Gmail 收件匣抓聯絡人」** — 原本的名稱看不出
  是在讀誰的信、讀來做什麼。
- **自訂網址精靈的收集設定要分析完才能動** — 順序上一定是「先分析、
  才知道有哪些欄位可以選、這個名錄有幾頁」。分析之前就讓它們可以填，
  剛好把因果顛倒過來。

### 修正

- **設定總覽仍顯示完整路徑** — 上一版只改到「資料庫」那一行，
  匯出／日誌／備份三行還是印著含使用者帳號名稱的完整路徑。
- **「刪除該日全部」按不下去** — 原本要求先在「收集日期」選一天才會啟用。
  改成永遠可以按：選了日期就刪那天，沒選就刪今天，按鈕文字跟著變。
  確認視窗的家數改成問資料庫，不是用畫面上的列數（同時下了關鍵字篩選時
  兩者不一樣，而刪除是刪整天）。
- **「套用」按鈕與旁邊的輸入框沒對齊**。
- **設定檔路徑那行的說明過時** — 現在總覽可以直接編輯了。

---

## 1.1.0 — 2026-08-05

### 新功能

- **反饋頁**（側邊欄）— 直接在程式裡把問題回報給作者，可以附截圖。
  已設定 Gmail 就直接寄出，還沒設定就開啟你自己的郵件軟體並填好內容。
- **信件附件** — 寄開發信時可以附加檔案，單封信合計上限 20 MB。
  附件庫可以改顯示名稱、寫備註、看用過幾次；被自動排程引用的檔案刪除前會警告。
- **自動排程擴充** — 除了爬取，現在也能排程寄信，或「先爬取再把名單寄出去」。
  新增每月排程（可指定幾號），以及「寄給誰」的篩選條件。
- **公司頁依收集日期檢視** — 下拉選單列出每一天各收集了幾家，
  選定某天後可以整批刪除（某次爬壞時的清理入口）。
- **爬取欄位選擇** — 在「自訂網址」精靈裡勾選這個來源要收集哪些欄位。
- **來源預設產業** — 名錄網站多半是一個分類一頁，分類本身就是產業。
  填在來源上，頁面沒提供產業時自動補上。
- **刪除備份** — 設定頁可以刪掉不要的備份。
- **匯入範例檔** — 下載可以直接填寫的 Excel，附逐欄說明分頁。

### 修正

- **爬取只到第 10 頁就停** — 全域的頁數上限會蓋掉來源自己明確設定的值。
  現在全域值只是「來源沒指定時的預設」，來源說 24 頁就是 24 頁。
- **數字分頁抓不到** — 以前只認得寫著「下一頁」的連結。
  台灣的名錄網站絕大多數是「1 2 3 4 5」這種純數字分頁，
  現在會認出來，並且直接告訴你這個名錄總共有幾頁。
- **公司名稱誤抓成「詳細資料」** — 每張卡片都有的樣板連結命中率是 100%，
  比真正的公司名還高。改用「每一列都不一樣」當主要判準。
- **macOS 介面跑位** — 版面尺寸原本全是照 Windows 字型寫死的。
  改為從字型度量推導，各平台也改用自己的系統介面字。
- **下拉選單文字被截斷**（「電子零組件」變成「電...件」）。
- **控制項沒對齊** — QSpinBox 比其他控制項高 4px。
- **介面顯示完整檔案路徑** — Windows 的路徑含使用者帳號名稱，
  而這些畫面常被截圖分享。改成只顯示相對路徑。
- **缺少的相依套件** — `requirements.txt` 漏了 PySide6 與 cryptography。
  後者缺席時不會當機，只是加密安靜失效。
- **Gmail 帳號狀態顯示兩次「尚未設定」**。
- **「清除」按鈕語意不明** — 改名為「清除篩選」，它只清搜尋條件、不刪資料。

---

## 1.0.0 — 2026-08-03

第一個版本。

- 爬取公開的台灣 B2B 廠商名錄，貼上網址即可自動辨識頁面結構
- 資料清理：正規化、格式驗證、MX 查詢、去重合併
- 公司與聯絡人管理，匯入／匯出 Excel、CSV、JSON
- 用自己的 Gmail 寄送開發信，含樣板、變數、演練模式與多重防護
- 個資欄位 AES-GCM 加密，金鑰存在系統憑證保管庫
- 自動排程爬取、自動備份
