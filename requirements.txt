# Taiwan B2B CRM - runtime dependencies
# Python 3.12+

# --- crawling ---
playwright>=1.47.0
beautifulsoup4>=4.12.3
lxml>=5.2.0
httpx>=0.27.0
tenacity>=8.3.0
# Verify HTTPS against the OS trust store. Many Taiwanese sites use TWCA
# certificates that Python's bundled store rejects but Windows accepts.
truststore>=0.9.0

# --- data / export ---
pandas>=2.2.0
openpyxl>=3.1.2

# --- persistence ---
SQLAlchemy>=2.0.30

# --- validation / config ---
pydantic>=2.7.0
PyYAML>=6.0.1
python-dotenv>=1.0.1
dnspython>=2.6.1

# --- secrets ---
# Stores the Gmail app password in the OS credential vault (Windows Credential
# Manager / macOS Keychain) instead of a plaintext .env in the project folder.
keyring>=25.0.0

# --- cli / logging / ui ---
typer>=0.12.3
rich>=13.7.0
loguru>=0.7.2
customtkinter>=5.2.2
